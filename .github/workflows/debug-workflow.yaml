name: Debug

on: 
  pull_request:
    branches:
      - master
   


jobs:
  restore: 
    name: Restore
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{ github.sha }}

      - run: ll