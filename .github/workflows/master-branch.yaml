name: Master branch 0.1.0

on: 
  push:
    branches:
      - master
   

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - run: echo ${{ github.run_id }}
      - run: echo ${{ github.run_number }}